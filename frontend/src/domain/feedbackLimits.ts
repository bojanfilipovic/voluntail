/**
 * Peer feedback field limits — must stay aligned with backend
 * (`SUGGESTION_MAX_*` in FeedbackRoutes.kt).
 */
export const SUGGESTION_MAX_MESSAGE_LENGTH = 4000
export const SUGGESTION_MAX_CONTACT_LENGTH = 100
