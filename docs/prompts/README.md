# Prompts (Voluntail)

| File | Use |
|------|-----|
| [original-baseline-prompt.md](original-baseline-prompt.md) | The **earlier** handoff you used when kicking off the roadmap/MVP plan (archived for history). |
| [VOLUNTAIL_PROMPT.md](VOLUNTAIL_PROMPT.md) | **Canonical** prompt: **select all → copy → paste**; add your work only at the **end** under **# START HERE** → **Current task:**. The **(1)–(4) agent runbook** is at the **bottom** after that. Everything above is static reference. |

**Update rule:** when architecture, environment, **security boundaries**, or **public API** change in a material way, edit `VOLUNTAIL_PROMPT.md` and add or amend a record in [`../adr/`](../adr/) (see that folder’s README). Optionally note the `git` commit for the code change.

The **Plan remainder** (short wins, longer features, and icebox reminders *not* fully itemized in `VOLUNTAIL_PROMPT.md`) is in the Cursor plan `voluntail_mvp_assessment_4b72e3c6.plan.md` under the section *Plan remainder* (file may live under your user’s `.cursor/plans/` or a copy in-repo if you add one).
